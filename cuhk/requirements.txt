# CUHK 校园地图数据管线依赖（Python 3.11+）
# 安装：pip install -r cuhk/requirements.txt
# 注意：osmnx 必须钉在 2.0.7——管线使用了其私有模块 ox._errors（osmnx 2.x 捕获空结果的唯一途径）
osmnx==2.0.7
geopandas>=1.0
shapely>=2.0
numpy>=1.26
matplotlib>=3.8
scipy>=1.11
PyYAML>=6.0
requests>=2.31
pytest>=8.0
